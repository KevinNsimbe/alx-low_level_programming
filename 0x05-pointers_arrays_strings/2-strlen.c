#include "main.h"
/**
 * _strlen - return the length of a string.
 * @s: char to check
 * Return: 0
 */

int _strlen(char *s)
{
	int x = 0; /* valuable to start counter */

	for (; *s++;)
		x++;
	return (a);
}
